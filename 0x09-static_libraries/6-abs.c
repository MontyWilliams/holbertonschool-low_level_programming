#include "main.h"
/**
* _abs - Initiates  function
* @c: this is the holder variable
* Description:  tske any single integer and return absolute value
* Return: the absolute value of int
*/
int _abs(int c)
{
if (c < 0)
{
(c = -c);
}
return ((int) c);
}
