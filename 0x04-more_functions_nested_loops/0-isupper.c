#include "main.h"
/**
 * _isupper - checks if a Char is uppercase
 * @c: parmeter for function
 * Description: bootleg functions that checks if a value is upper case
 * Return: 1 if upper, 0 if not
 *
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')

return (1);
else
return (0);
}

