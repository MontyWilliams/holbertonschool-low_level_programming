#include <stdio.h>
#include <stdlib.h>
#incl
ude "search_algos.h"

/**
 * binary_search - binary search algo
 * @array: array of
 * @value: value to search
 * @size: size of
 * description: search bru bru
 * Return: Always EXIT_SUCCESS
 */

int binary_search(int *array, size_t size, int value);
{
  int left = size[0];
  int right = (size - 1);
  int mid = (left + right) / 2;

  c = (a < b) ? a : b;                        +

}
